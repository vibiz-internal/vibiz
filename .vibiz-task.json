{
  "title": "Fixed Stripe build-time configuration issues",
  "description": "Updated Stripe initialization to use the installed SDK apiVersion and lazy-create the client so the production build succeeds without touching unrelated UI files.",
  "status": "ok",
  "database": { "kind": "postgres" }
}
